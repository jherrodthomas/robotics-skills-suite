# Week 2026-W37 — Plan (autonomous PLAN run)

Generated: 2026-09-07 (Monday)

## Recap (past 7 days)

```
9f42e7f auto(triage): sweep 20 issues, fire 30-day rule on #38-#41, flag edition-audit false positives
45fdb14 auto(release): cut v2026.09.W1 snapshot for W36, note missing W35 tag
0f366be auto(docs): add W36 changelog, backfill missing W35, fix three stale README editions
d88845e auto(polish): add LOTO status, delivered-vs-demonstrated, conditional retraining to operator-training-matrix pair
159b5c5 auto(polish): restore 2026-06-18 entry in iso9283 polish log, append rather than overwrite
e9cdbaa auto(polish): define AP vs RP, cube and cycle conditions, uncertainty ratio, FAT boundary in iso9283 pair
b52ef25 auto(polish): retire EOL Iron baseline from nav2-config pair, close urdf-tf-nav2 chain
3ea30de auto(plan): seed W36 with three inverted-pair targets, commit all-38 edition audit
```

**W36 cleared all three targets — fourth consecutive clean week.** #54 (nav2-config) Tuesday, #55 (iso9283) Wednesday, #56 (operator-training-matrix) Thursday. Four zero-carryover weeks at capacity 3. **W37 holds at 3.**

Two things worth carrying forward from the week:

- The `urdf -> tf -> nav2` sequence opened at W34 **closed on schedule** at b52ef25, and closing it turned up an EOL ROS 2 distro baseline the pair had been carrying. Sequencing frame-defining skills before frame-consuming ones paid the way W35 argued it would.
- 159b5c5 is a self-correction: the Wednesday run **overwrote** rather than appended the `iso9283` polish log and lost the 2026-06-18 entry, and the same run put it back. The polish-log contract is *append*. Worth a line here because it is the first time this task has damaged its own history, and it was recoverable only because the entry was still in git.

## Target selection — what the priority rules actually returned

- **Rule (a), skills referenced by open issues:** selects nothing, for the **sixth** consecutive week. All 20 open issues (#37-#56) describe work that has already landed. See Notes.
- **Rule (b), orphan builders:** selects nothing. 38/38 paired since W29.
- **Rule (c), least-recently-touched:** returns the surviving 2026-05-03 import cohort. Two of three targets come from here.

### What the measures say now

The body-thinness ranking that drove W36 is nearly exhausted — every builder under 10 body lines has now been polished except two:

| Builder body | Reviewer body | Skill | Status |
| --- | --- | --- | --- |
| **9** | 11 | `ros2-system-architecture` | **W37 target #58** |
| **9** | 14 | `robot-field-acceptance` | W38 lead |
| 11 | 9 | `interlock-estop-architecture` | not inverted — first non-inverted pair in the ranking |

`interlock-estop-architecture` at 11/9 is the boundary: it is the first pair where the builder out-weighs its reviewer, which means the thinness sort has run out of the defect it was detecting. **After `robot-field-acceptance` lands, thinness stops being a useful selector** and the ratio measure takes over. That measure's current worst case is this week's #57.

## Targets (3)

### 1. `iso10218-compliance-matrix` pair — _compliance_ — [#57](https://github.com/jherrodthomas/robotics-skills-suite/issues/57)

23-line builder body against a 36-line reviewer: **the worst builder/reviewer substance ratio in the suite**, on the compliance keystone skill. The builder body is boilerplate — "Use this skill when the user mentions ISO 10218, or related requirements" — while the reviewer runs a real 2025-edition argument.

The ratio measure and the edition audit agree on this pair, from different directions: `audit_pair_editions.py` independently flags it because the **reviewer pins ISO 10218-1:2025 and ISO/TS 15066:2016 and the builder pins neither**. The thin half is the unpinned half. That is the strongest single signal any selector has produced this quarter, and it is why this is the Tuesday slot rather than the thinner `ros2-system-architecture`.

- **Definition of done:** the builder states what the matrix produces — one row per ISO 10218-1/-2 clause with a conformance verdict, evidence reference and responsible party; the **-1 (robot) vs -2 (robot system and integration) split explicit**, since a single undifferentiated matrix is the failure mode this skill exists to prevent; integrator vs manufacturer duty allocation named; builder pins 10218-1:2025, -2:2025 and 15066:2016 to match the reviewer; boundary against `ansi-r1506-compliance-matrix` drawn — the ISO and ANSI/A3 routes are not interchangeable. Reviewer in lockstep.
- **Take this Tuesday.** Compliance is safety-critical: **the mandatory edition-verification step fires.**

### 2. `ros2-system-architecture` pair — _ros2_ — [#58](https://github.com/jherrodthomas/robotics-skills-suite/issues/58)

9-line body against an 11-line reviewer; last touched 2026-07-16 (53 days). The second-to-last inverted pair.

- **Definition of done:** node/topic/service/action decomposition given as a method rather than a glossary; QoS profile selection tied to data class (sensor stream vs command vs latched config) with reliability/durability/history named; executor and callback-group choice with the reentrancy trap; lifecycle states and bringup ordering; **frame vocabulary reconciled with `tf-tree-spec` (2026-08-27), link names with `urdf-model-spec` (#49), Nav2 interface with `nav2-config` (#54)**; parameter and namespace conventions for multi-robot. Reviewer in lockstep.
- **Take this Wednesday.** This builder sits *above* the three skills the last three weeks polished and is where their vocabulary has to reconcile — the natural cap on the urdf/tf/nav2 sequence, taken one week after it closed while those decisions are still current. Not safety-critical, so the edition step does not fire; **the distro baseline must still be checked against what b52ef25 put in place of the retired Iron baseline.**

### 3. Edition-asymmetry sweep — 16 claims across 8 pairs — [#59](https://github.com/jherrodthomas/robotics-skills-suite/issues/59)

W35's all-38 audit measured the drift thesis and found the residue is **missing** edition claims, not wrong ones: one half pins, the other names the standard bare. W36 named this a W37 candidate at bounded scope. This is that slot, and it retires a piece of debt that has been described in three consecutive plans without being paid.

Scope is fixed by the script, not by judgement: `cobot-hand-guiding` (3), `interlock-estop-architecture` (4), `robot-cell-layout` (3), `ssm-plan` (3), `iec62443-risk-assessment` (1), `iso3691-4-risk-assessment` (1), `robot-cell-scope` (1), `robot-sop` (1). **`iso10218-compliance-matrix` is out of scope** — its 2 asymmetries close on Tuesday under #57 and must not be double-touched. **`declaration-of-conformity` is out of scope** — its ANSI R15.06-2012 citation sits inside a *negative* check and is correct behaviour, verified by hand at W35.

- **Definition of done:** each listed citation in the unpinned half carries its edition year. **No body rewrites.** Re-run the audit after: ASYMMETRY should list only `declaration-of-conformity`; MISMATCH should still show only the benign `robot-cell-scope` supersession sentence.
- **Take this Thursday.** Every pair in scope is safety-critical, so **the edition step fires for all of them** — which is the entire content of the task, so verification is not an add-on this week, it *is* the work.
- **Deviation, stated openly:** POLISH takes ONE skill; this touches 8 reviewer files. Justified because the edit is mechanical and identical in each, the blast radius is one line per claim, and the audit script verifies the result independently. **If any pair needs a judgement call about which edition applies, drop that pair, do the other seven, and carry it to W38 as its own target.** Descoping is the correct move there, not reasoning it out under a themed slot.

## Domain-spread check

Picks land on **compliance**, **ros2**, and a cross-cutting sweep spanning amr / cell-design / cobot / cybersecurity / foundation / operational. No domain carries two targets. ros2 repeats from W36 for the third week — and this is the **last** ros2 target justified by the urdf/tf/nav2 sequence. From W38, ros2 competes on merit like everything else.

## Standards / version refresh notes for Tue-Thu POLISH runs

| Target | Editions / baselines to verify and date |
| --- | --- |
| #57 iso10218-compliance-matrix | ISO 10218-1:**2025** and -2:**2025** current · ISO/TS 15066:**2016** still a TS and still current · ANSI/A3 R15.06-**2025** (2012 (R2017) superseded) |
| #58 ros2-system-architecture | ROS 2 distro baseline **must match what b52ef25 set** when it retired Iron from `nav2-config` · REP-**105** frame IDs as fixed by #52 · QoS/lifecycle API names against that distro |
| #59 asymmetry sweep | Every year in the issue table, against the issuing body: ISO 10218-1/-2:**2025** · ISO/TS 15066:**2016** · ISO 13849-1:**2023** · ISO 13850:**2015** · ISO 14119:**2013** · ISO 13855:**2024** · ISO 13857:**2019** · ISO 3691-4:**2023** · ANSI/A3 R15.06-**2025** · IEC 62443 part+edition |

**Do not assume the builder is right.** Fifth consecutive restatement, and #59 is the week it earns its keep: the sweep's whole job is writing years into files, so a wrong year here propagates rather than sits still.

## Notes

- **Capacity stays at 3.** W33: 3/3 · W34: 3/3 · W35: 3/3 · W36: 3/3. Four consecutive zero-carryover weeks at the same number.
- **Rule (a) is now six-for-six dead, and the backlog grew again.** Three new issues (#57-#59) put the open count at **23** while real outstanding work is 3. The Sunday TRIAGE run has fired the 30-day quiet notice on #38-#41 and cannot go further — this task never closes issues by design. **Ninth consecutive request: close #37-#56 by hand.** Until that happens the highest-priority selection rule in this task contributes nothing, and the triage runs are commenting on work that shipped weeks ago.
- **Second request for the same task-file edit:** the standing reference table lists `ISO 3691-4:2020`; the current edition is **2023**, which the builder already tracks correctly. #59 writes 2023 into the `iso3691-4` reviewer this week, so after Thursday the task file will be the *only* artifact in the loop still saying 2020 — and the one most likely to make a future run "correct" a right answer into a wrong one.
- **The polish-log contract is append, not overwrite.** 159b5c5 recovered a lost entry this week. Noted here so the next POLISH run reads it before opening a log file.
- **W38 candidates, in order:** (1) `robot-field-acceptance` — last inverted pair, 9/14, at the 2026-05-03 import baseline and among the oldest untouched files in the repo; (2) `iec62061-sil` at 20/36, the next-worst ratio after this week's #57; (3) `interlock-estop-architecture`, notable as the first pair where the *builder* out-weighs the reviewer — the reviewer is the thin half, which is a defect this task has not yet gone looking for and may exist elsewhere. **A reviewer-thinness ranking is the natural successor measure once #58 and `robot-field-acceptance` retire builder-thinness; running it is a cheap W38 PLAN job.**
- Staleness: 27 of 38 builders yellow at 30+ days, 0 red. #57 and #58 clear two; #59 touches reviewers only and does not move builder dates. At 2-3 builders/week the backlog clears around W48 — later than W36's W44 estimate, because a themed sweep buys correctness rather than freshness. That is the right trade this week and not one to repeat weekly.
