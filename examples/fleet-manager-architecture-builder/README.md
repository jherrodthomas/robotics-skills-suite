# Example — `fleet-manager-architecture-builder.skill`

**What this skill produces:** An audit-ready AMR Fleet Manager architecture workbook (12 tabs) capturing fleet manager software responsibilities, traffic management algorithm, charging coordination, mission dispatch, robot health monitoring, deadlock resolution, and MQTT/REST/VDA 5050 protocol integration.

**Typical input shape:** JSON describing the fleet (robot types and count, site map zones, charging infrastructure, mission types, protocol stack).

**Expected output:** Multi-tab xlsx architecture workbook with per-topic worksheets and a References tab.

**Sample I/O:** See `sample_input.json` and `sample_output.xlsx` (to be added).

_Stub generated by the autonomous DOCS run on 2026-07-24. Note: the packaged generator is currently a placeholder (see docs/skill-polish-log/fleet-manager-architecture-builder.md); replace this stub with a real worked example once the generator is implemented._
