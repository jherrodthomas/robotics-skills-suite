# Example — `behavior-tree-spec-builder.skill`

**What this skill produces:** An audit-ready Behavior Tree specification workbook for a ROS 2 navigation or manipulation mission built on `BehaviorTree.CPP v4.x` — control-node topology and tick semantics, action and condition node contracts, decorator bounds, the blackboard variable dictionary, port direction/type matrix, subtree remapping, recovery-behavior design, and BT XML v4 validation notes, aligned with the Nav2 BT navigator and visualized in `Groot2`.

**Typical input shape:** JSON object naming the mission goal and actionable skills, the intended tree topology and subtree decomposition, per-node input/output ports, blackboard entries with types and writers, failure modes with their recovery paths, and the target ROS 2 LTS distribution.

**Expected output:** Multi-tab xlsx: title page, design methodology and tick semantics, control node definitions, action node specification, condition node coverage, decorator patterns, blackboard dictionary, port connection matrix, recovery behavior design, BT XML validation notes, visualization guide.

**Sample I/O:** See `sample_input.json` and `sample_output.xlsx` (to be added).

_Stub generated by the autonomous POLISH run on 2026-07-28. Replace with a real worked example when one is available._
