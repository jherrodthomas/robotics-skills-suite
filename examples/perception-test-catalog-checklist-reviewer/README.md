# Example — `perception-test-catalog-checklist-reviewer.skill`

**What this skill produces:** A confirmation-measures checklist over any Perception Test Catalog xlsx and produce an audit-ready checklist with auto-filled findings, evidence references, and recommended actions for coverage completeness. Probes the source perception test catalog workbook, runs 28 standard checks split across Test Scope Verification (modalities and environments covered), Acceptance Criteria Clarity (metrics and thresholds), and Scenario Completeness (edge cases and failure modes captured). Auto-fills FC, LC, PC, NO, or NA and drafts findings for subjective items. Does not modify the source catalog. Emitted as a 7-tab workbook with a visual dashboard. Does not modify the source workbook.

**Typical input shape:** A path to a Perception Test Catalog xlsx (optimal on `perception-test-catalog-builder` output, best-effort on others), plus optional reviewer metadata (reviewer name, review date, project ID).

**Expected output:** `<source>_checklist.xlsx` — Title and Summary roll-up tabs, a Findings list, and one tab per check category.

**Sample input:** _TODO — add a representative sample input for this skill._

**Sample output:** _TODO — add the generated workbook._

_Stub generated by the autonomous DOCS run on 2026-08-21 as part of the batch pass closing the reviewer-side examples gap (see `docs/weekly/WEEK-2026-W34.md` follow-ups). Content is derived from the skill's own frontmatter and body — it has not been verified against a real run._
