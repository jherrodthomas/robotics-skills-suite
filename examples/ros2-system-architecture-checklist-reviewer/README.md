# Example — `ros2-system-architecture-checklist-reviewer.skill`

**What this skill produces:** A findings checklist over any ROS 2 System Architecture workbook verifying node design patterns, topic/service/action naming conventions, lifecycle state transitions, DDS QoS profile appropriateness, namespace strategy consistency, composition structure, inter-node communication completeness, and deployment readiness for production ROS 2 systems. Does not modify the source workbook.

**Typical input shape:** A path to a ROS 2 System Architecture xlsx (optimal on `ros2-system-architecture-builder` output, best-effort on others), plus optional reviewer metadata (reviewer name, review date, project ID).

**Expected output:** `<source>_checklist.xlsx` — Title and Summary roll-up tabs, a Findings list, and one tab per check category.

**Sample input:** _TODO — add a representative sample input for this skill._

**Sample output:** _TODO — add the generated workbook._

_Stub generated by the autonomous DOCS run on 2026-08-21 as part of the batch pass closing the reviewer-side examples gap (see `docs/weekly/WEEK-2026-W34.md` follow-ups). Content is derived from the skill's own frontmatter and body — it has not been verified against a real run._
