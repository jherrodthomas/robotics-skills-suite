# Week 2026-W39 — Plan (autonomous PLAN run)

Generated: 2026-09-21 (Monday)

## Recap (past 7 days)

```
5212f02 auto(triage): empty board — audit-driven repo triage finds 32 of 38 reviewers unimplemented
3291796 auto(release): cut v2026.09.W3 for W38, re-run audits, close ISO 13855 sweep
e8e512c auto(docs): add W38 changelog, date IEC 62443 parts and three new editions in README
7ac735d auto(polish): date IEC 62443 editions, split SL-T from SL-A/SL-C, add ZCR 3 partitioning to zone-conduit pair
dcf516f auto(polish): give robot-field-acceptance pair an OEE method and a conformity boundary
2048716 auto(polish): give ros2-system-architecture pair a method, rated checks, and a safety boundary
```

**W38 ran without a plan.** Monday 2026-09-14 did not fire, so there is no `WEEK-2026-W38.md`; the three POLISH passes above selected on the least-recently-touched fallback. They were good picks — `ros2-system-architecture` and `robot-field-acceptance` were the two designated W38 leads and `zone-conduit-plan` was a never-polished 2026-05-03 builder — but none had a written definition of done beforehand. **This plan does not backfill W38.** The week was worked; a retrospective plan would be fiction. It opens W39 explicitly and absorbs the two selection decisions W38 should have made.

Three things changed the ground under this plan, and all three are structural rather than incremental:

- **The board is empty.** All 46 issues were closed by hand on 2026-09-11, answering ten consecutive requests. Rule (a) is no longer *deadlocked* — it is simply *empty*, which is a different problem and this run's main job.
- **Builder-thinness is exhausted.** `robot-field-acceptance` (2026-09-16) was the last inverted pair. The selector that drove W33–W38 has no candidates left.
- **The largest defect in the repo has never been on the board.** Sunday's triage ran `audit_reviewer_impl.py` against the tree: **6 of 38 reviewers are implemented**, 16 are stub generators, 16 have no generator at all. STATUS.md's "38/38 paired (100%)" is true by filename and nominal for 32 pairs.

## Target selection — what the priority rules actually returned

- **Rule (a), skills referenced by open issues:** **0 open issues.** Returns nothing for the eighth consecutive week, for the opposite reason to the previous seven.
- **Rule (b), orphan builders:** nothing. 38/38 paired since W29.
- **Rule (c), least-recently-touched:** returns the residue of the 2026-05-03 import cohort — `perception-test-catalog` (140d) and `wireless-coexistence-plan` (140d) are the only two never-polished members left. One becomes Thursday.

With (a) empty and builder-thinness gone, this run stood up the successor selector three consecutive journals asked for, and **committed it** as `scripts/rank_reviewer_thinness.py` rather than deriving it in scratch space — same reasoning as `gen_status.py`.

### The successor measure, and the trap it nearly walked into

The obvious formulation is `reviewer_body - builder_body`. **It is wrong,** and running it proved so in one pass:

| Sort by delta | Sort by absolute reviewer size |
| --- | --- |
| 1. `dataset-documentation` (109/67, -42) | 1. `robot-cell-layout` (33/**6**) |
| 2. `nav2-config` (102/65, -37) | 2. `interlock-estop-architecture` (11/**9**) |
| 3. `tf-tree-spec` (76/46, -30) | 3. `safety-io-matrix` (13/**13**) |
| 4. `robot-cell-layout` (33/6, -27) | 4. `robot-acceptance-protocol` (13/20) |

The delta sort is dominated by *recently polished* pairs: `dataset-documentation` leads it only because its builder was grown to 109 lines, while its 67-line reviewer is among the most substantial in the repo. **Delta measures imbalance; it does not measure thinness.** Absolute reviewer body size is the signal, and reading the files confirmed it — `robot-cell-layout-checklist-reviewer` is one sentence of purpose plus a 3-item Standards list and **states zero checks**, while `safety-io-matrix` at 13 lines names real checks and dates its standards and is *healthy*. The script prints both columns, sorts on absolute, and says in its docstring that line count is a proxy and the last step is to read the file.

**Only 3 reviewers sit under 15 body lines.** Two of them are this week's Tuesday and Wednesday. This selector has a short runway and the plan should not pretend otherwise — see Notes.

## Targets (3 worked + 2 tracked)

### 1. `robot-cell-layout` pair — _cell-design_ — [#60](https://github.com/jherrodthomas/robotics-skills-suite/issues/60)

**6-line reviewer — the thinnest artifact in all 76 files** — against a 33-line builder, 81 days stale, carrying **3 of the 9 open edition asymmetries**. The description promises "structured pass-fail criteria"; the body delivers a topic list.

- **Definition of done:** reviewer states rated check groups — guarding distance *derived* per ISO 13855:2024 rather than asserted, guard selection and gap-vs-reach per ISO 13857:2019, egress and trapped-person escape from inside the safeguarded space, sensor field coverage against the actual restricted space, muting/blanking justification; **safeguarded vs restricted vs operating space** made explicit, since conflating them is the failure mode this pair exists to catch; three editions pinned to match the builder; boundaries drawn against `safety-io-matrix` and `interlock-estop-architecture`. Builder in lockstep.
- **Take this Tuesday.** Safety-critical: **the edition step fires.** The builder carries `['2011','2025']` for ISO 10218-2 — 2011 is superseded.

### 2. `interlock-estop-architecture` pair — _cell-design_ — [#61](https://github.com/jherrodthomas/robotics-skills-suite/issues/61)

9-line reviewer / 11-line builder — **the thinnest pair, both halves** — 95 days stale, **4 of the 9 asymmetries**, and top of Sunday's triage shortlist as the highest known-defect density in the repo. Six bare bullets; naming a topic is not a check.

- **Definition of done:** E-stop framed as a **complementary protective measure, not a risk reduction measure** — the single most common misuse of this document; **stop category 0/1/2 per IEC 60204-1:2018 kept distinct from PL/Category per ISO 13849-1:2023**, which this pair currently blurs; reset/restart interlock and the unexpected-start-up path; interlocking guard type and coding level per ISO 14119:2013 with the defeat-motivation assessment; dual-channel discrepancy monitoring and fault exclusion stated as a claim requiring justification; span-of-control and zone grouping. Both halves pinned. Builder in lockstep.
- **Take this Wednesday.** Safety-critical: **the edition step fires.** Watch the open **ISO/DIS 13849-2** supersession (enquiry stage; the 2012 edition is cited across this repo).

### 3. `perception-test-catalog` pair — _ai-ml_ — [#62](https://github.com/jherrodthomas/robotics-skills-suite/issues/62)

**The oldest untouched builder in the suite** — 2026-05-03, 140 days, no polish log ever opened. Its reviewer is **tier A** (28 checks, real generator), one of only six, which is precisely why it earns a slot: tightening a documented contract that the code actually keeps strengthens a real promise instead of inflating an empty one. `zone-conduit-plan` (09-17) is the precedent.

- **Definition of done:** builder states the row contract — one perception test case per row with an **ODD condition**, an expected outcome, and a pass threshold expressed as a **rate with a confidence interval and a stated sample size** rather than a bare percentage; **nominal / degraded / adversarial** condition split explicit; the **false-negative vs false-positive consequence asymmetry** named, since a missed human and a phantom obstacle are not symmetric in a robot cell; boundaries against `model-card` and `dataset-documentation`; tier-A check table **re-keyed to the requirement each check implements**, per the outstanding zone-conduit follow-up. Reviewer in lockstep.
- **Take this Thursday.** Not safety-critical, so the mandatory edition step does not fire — but any standard newly cited still gets dated. `robot-field-acceptance` introduced six citations under exactly this exemption on 09-16 and the voluntary check caught two real errors.

### 4–5. Tracked debt, not this week's work — [#63](https://github.com/jherrodthomas/robotics-skills-suite/issues/63), [#64](https://github.com/jherrodthomas/robotics-skills-suite/issues/64)

**Stated deviation:** PLAN normally files exactly three `weekly-target` issues. This run files **five issues, three of them targets.** #63 and #64 are labelled as debt rather than targets and are deliberately *not* scheduled.

- **[#63] 32 of 38 reviewers ship a stub or no generator.** Reported at six consecutive snapshots with no decision. Implementing 32 generators is a build, not a polish, and this task is instructed to stop short of large refactors — so the issue presents three options (authorize an implementation week / mark the contract honestly / add a reviewer-implementation column to STATUS.md) and asks the human to choose. **The third is cheap and worth doing regardless**, because the 100% pairing headline currently reads as 100% working.
- **[#64] The edition-asymmetry sweep, re-filed.** #59 was closed in the 09-11 bulk close **without the work being done** — three journals flagged it. The audit now reports **2 MISMATCH + 9 ASYMMETRY**. Tuesday and Wednesday close 7 organically; #64 carries the remainder so it stops living only in journal entries.

Filing debt as issues is the answer to Saturday's follow-up that PLAN "has only staleness + domain rotation as selectors". **From W40 rule (a) has real outstanding work to return** — for the first time since W31, and the point of doing it this way rather than by inventing a new measure.

## Domain-spread check

Worked targets land on **cell-design**, **cell-design**, **ai-ml**. **Two in one domain, and that is a deviation from the spread rule — taken deliberately.**

Justification: the reviewer-thinness ranking's top two *both* fall in cell-design, which is a finding about that domain rather than a scheduling accident — it was imported as a block and its reviewers were never written past a topic list. Taking them in consecutive slots also closes **7 of the 9 remaining edition asymmetries** as a side effect of real polish passes, which is strictly better than the mechanical sweep #59 tried and failed to be. Thursday deliberately swings to ai-ml, and **no ros2 target this week** — ros2 took three consecutive passes through W38 and now competes on merit like everything else.

`iso15066-biomechanical-limits` (cobot, 108d, safety-critical) was the leading spread-preserving alternative and is held as the **W40 lead**.

## Standards / version refresh notes for Tue–Thu POLISH runs

| Target | Editions / baselines to verify and date |
| --- | --- |
| #60 robot-cell-layout | ISO 13855:**2024** (retitled at that edition — confirmed 09-16) · ISO 13857:**2019** · ISO 10218-2:**2025** (builder still carries superseded **2011**) · OSHA 1910.147 · ANSI Z535 |
| #61 interlock-estop-architecture | ISO 13849-1:**2023** · ISO 13850:**2015** · ISO 14119:**2013** · ISO 10218-1:**2025** · IEC 60204-1:**2018** · **ISO/DIS 13849-2 at enquiry** — will supersede the 2012 edition cited repo-wide |
| #62 perception-test-catalog | No mandatory step (ai-ml). Date anything newly cited. ISO/TS 15066:**2016** if referenced — see correction below |

**Do not assume the builder is right.** Sixth consecutive restatement, and it has now earned its keep three weeks running: 09-08 caught the ISO/TS 15066 status, 09-16 caught the ISO 13855 retitle and the ISO/DIS 13849-2 supersession, 09-19's sweep confirmed the 13855 fix had landed everywhere.

## Notes

- **Capacity holds at 3.** W33–W36 ran 3/3. W37 lost Thu–Sun. W38 ran 3 unplanned. Three is the number the loop sustains when it fires.
- **Schedule reliability is now the top process risk, above any content debt.** Missed slots: W35 Fri–Sun, W37 Thu–Sun, W38 Mon, plus 09-13 and 09-14. That is **three missed weekends and the first missed weekday** in four weeks, and it is why W38 ran unplanned. **Human: verify the 7:30 schedule actually fires** — raised by the last three runs and unanswered. Every other item in this plan is recoverable; a loop that does not run is not.
- **The successor selector has a ~2-week runway.** Only 3 reviewers sit under 15 body lines and two are taken this week. `safety-io-matrix` is third and is **not a defect** — 13 lines, real checks, dated standards. **So from W40 reviewer-thinness is effectively spent too**, and the honest read is that both thinness measures were detecting the same thing: unpolished imports. The durable successor is not another line-count measure — it is **#63's tier ranking**, which sorts by whether the code exists at all. W40's PLAN should adopt tier (C → B → A) as the primary selector.
- **Two standing corrections to the task file, unactioned.** (1) The reference table says `ISO 3691-4:2020`; current is **2023**, which the builder already tracks correctly — **fourth request**. (2) The `ISO/TS 15066:2016` entry should read *"current edition, being superseded by ISO/AWI 15066-1"*, not "still current" — **third request**. Both are now the only artifacts in the loop still carrying the wrong status, which makes them the most likely source of a future run "correcting" a right answer into a wrong one.
- **The polish-log contract is append, not overwrite** (159b5c5 recovered a lost entry). Read before opening a log file.
- **Frontmatter validation is part of verify.** A `.skill` is a zip, so broken YAML does not appear in `git diff` — `yaml.safe_load` both halves after every edit (zone-conduit, 09-17).
- Sweeps queued and still untouched: **15 skills advertising a phantom `references/` directory** (now inside #63), ISO/DIS 13849-2 watch, and a leaked `/sessions/…/automotive-skills-suite/…` absolute path in six placeholder script comments.
- **Duplicate labels** `bug`/`skill-bug` and `documentation`/`docs` — human decision, raised 09-20.
- Staleness: 29 of 38 builders yellow at 30+ days, 0 red. #60/#61/#62 clear three. At 3/week the backlog clears around **W49**.
- **W40 candidates, in order:** (1) `iso15066-biomechanical-limits` — cobot, 108d, safety-critical, the spread-preserving pick held from this week; (2) `wireless-coexistence-plan` — the last never-polished 2026-05-03 builder; (3) whichever tier-C reviewer #63 authorizes first, if the human answers.
