# Week 2026-W35 — Plan (autonomous PLAN run)

Generated: 2026-08-24 (Monday)

## Recap (past 7 days)

```
7d0171c auto(triage): audit all 76 descriptions, bound defect to five, find edition drift
3b13d45 auto(release): tag v2026.08.W4 snapshot, write release notes, refresh STATUS
46166a8 auto(docs): close examples coverage to 76/76, commit STATUS generator, W34 changelog
662de6b auto(polish): give dataset-documentation a citable ID, date governance baseline, fix reviewer tab claims
df811d4 auto(polish): baseline urdf-model-spec pair on ROS 2 distro, fix stale Ignition/Gazebo Classic
8ba1789 auto(polish): anchor robot-hil-test-catalog pair on ISO 13849-1:2023/-2:2012, add HIL-FAT boundary
4dc22b1 auto(plan): seed W34 with three import-baseline targets across v&v, ros2, ai-ml
```

**W34 cleared all three targets — second consecutive clean week.** `robot-hil-test-catalog` (#48) landed Tuesday, `urdf-model-spec` (#49) Wednesday, `dataset-documentation` (#50) Thursday, in exactly the order the plan sequenced them. Capacity 3 has now produced zero carryover twice running. **W35 holds at 3.**

Friday's DOCS run closed examples coverage to 76/76 and — more importantly — committed `scripts/gen_status.py`, ending the three-times-repeated re-derivation of the STATUS logic. Sunday's TRIAGE spent its slot on the description audit rather than on labels, and that audit is what sets this week's agenda.

## Target selection — what the priority rules actually returned

- **Rule (a), skills referenced by open issues:** selects nothing, for the fourth consecutive week. All 14 open issues (#37–#50) describe work that has already landed. The backlog is a close-confirmation queue, not a work queue.
- **Rule (b), orphan builders:** selects nothing. 38/38 paired since W29.
- **Rule (c), least-recently-touched:** eight builders remain at the **2026-05-03 import baseline** (113 days). This is where two of three targets come from.

The third target comes from Sunday's audit, which found a bounded, verified defect that no priority rule would have surfaced. It is taken first.

## Targets (3)

### 1. Rewrite five reviewer descriptions — generator framing, broken trigger, stale editions
_compliance (x4) + foundation (x1)_ — [#51](https://github.com/jherrodthomas/robotics-skills-suite/issues/51)

Filed verbatim from the block in `docs/triage/TRIAGE-2026-08-23.md`. **Independently reproduced by this run** before filing: all five and only five reviewer descriptions open with a generator verb, all five carry the broken `to review the user mentions` trigger clause, and no sixth case exists in the 76.

Scope is frontmatter `description:` only — five files, no body changes.

- **Definition of done:** each of the five reviewer descriptions (a) opens by describing a *review*, not a generation; (b) carries a grammatical "Use this skill when the user wants to review/audit…" trigger clause; (c) cites the same standard edition as its paired builder.
- **Sequence:** `ansi-r1506` → `iec62061` → `iso13849` → `iso12100` → `iso10218`.
- **Take this Tuesday.** `ansi-r1506-compliance-matrix-checklist-reviewer` cites **ANSI/RIA R15.06-2012 (R2017)** while its own builder, one file away, correctly cites **ANSI/A3 R15.06-2025** and notes the 2012 edition is superseded. That is a reviewer advertised against a superseded safety standard; it is the only item this week with a wrong-edition safety claim and it goes first.
- **Domain-spread rule is waived for this slot** — the defect is the theme, not the domain. One slot, not five.

### 2. `tf-tree-spec-builder.skill` — _ros2_ — [#52](https://github.com/jherrodthomas/robotics-skills-suite/issues/52)

At import baseline; 14-line body, fourth-thinnest builder in the repo.

- **Definition of done:** frame hierarchy stated as a worked tree (`map` → `odom` → `base_link` → sensor/tool frames) rather than named in the abstract; REP-105 frame semantics and REP-103 units/axis conventions cited by number and applied, not merely referenced; static vs dynamic transform split explicit with the publisher named per edge; update-rate and staleness-tolerance columns defined; `ros2_control` and URDF link-name agreement with the now-polished `urdf-model-spec` pair. Reviewer in lockstep.
- **Why tf before nav2:** carried straight from W34's follow-up. Nav2 config references frames that the TF spec defines; doing nav2 first means fixing vocabulary twice. `urdf-model-spec` was polished 5 days ago and its link-naming decisions are still fresh — this is the cheapest window to reconcile the two.

### 3. `eoat-spec-builder.skill` — _cell-design_ — [#53](https://github.com/jherrodthomas/robotics-skills-suite/issues/53)

At import baseline. **The thinnest skill in the repo: a 9-line body, with a 15-line reviewer.**

- **Definition of done:** payload accounting corrected to the full chain (tool mass + workpiece + tool-changer + inertia at the wrist, against the manufacturer's rated payload/torque envelope, not just a nominal kg figure); gripper-force and vacuum-holding calculations carry a stated safety factor and a loss-of-power retention requirement; electrical/pneumatic interface rows are specific enough to procure against; **safety interlocks tied to the `interlock-estop-architecture` and `safety-io-matrix` pairs** rather than named generically; drop-on-power-loss and drop-on-e-stop behaviour stated explicitly. Reviewer in lockstep.
- **Why:** at 9 body lines this is the least-substantive file in the suite, and it sits in a safety-critical domain where the thin part is the interlock behaviour. It is also the last cell-design builder at import baseline.

## Domain-spread check

Three picks across **compliance/foundation (themed), ros2, cell-design**. No domain carries two targets. W34 spent its slots on v&v, ros2, ai-ml; the only repeat is ros2, and that repeat is deliberate — it is the second half of the urdf→tf sequencing decided last week, taken while the upstream work is still fresh.

The spread rule is waived only for target 1, and only because that target is defined by a defect class rather than a domain. Targets 2 and 3 obey it normally.

## Standards / version refresh notes for Tue–Thu POLISH runs

Targets 1 and 3 are both in the safety-critical set, so the **mandatory edition-verification step fires for both**.

| Target | Editions to verify and date |
| --- | --- |
| #51 five reviewers | ANSI/A3 R15.06-**2025** (2012 (R2017) superseded) · IEC 62061:**2021** (SRECS -> **SCS**; the reviewer still says "safety-related *electrical* control systems") · ISO 13849-1:**2023** · ISO 12100:**2010** · ISO 10218-1/-2:**2025** |
| #52 tf-tree-spec | REP-**105** frame IDs · REP-**103** units/axis · ROS 2 distro baseline consistent with what #49 set on `urdf-model-spec` |
| #53 eoat-spec | ISO 10218-2:**2025** (integrator duties incl. end-effector) · ISO/TS 15066:**2016** if cobot grippers are in scope · ISO 13849-1:**2023** for interlock PL claims · EU Machinery Regulation **2023/1230** if a conformity route is cited |

**Do not assume the builder is right.** W34's lesson, restated by Sunday's audit: earlier polish passes anchored builders to current editions and left reviewers behind. Check both halves of every pair against the standard, not against each other.

## Notes

- **Capacity stays at 3.** W30: 2 cleared · W31: 2 · W32: 3 · W33: 3/3 · W34: 3/3. Two consecutive zero-carryover weeks at 3 is the observed rate; planning above it manufactures noise.
- **Rule (a) is now four-for-four dead and needs one human action to revive.** Three new issues (#51–#53) push the open count to 17 while real outstanding work is 3. Until #37–#50 are closed by hand, the highest-priority selection rule in this task contributes nothing. **Seventh consecutive request.**
- **The most valuable check found so far is builder<->reviewer edition agreement.** Sunday's audit found drift in four of five pairs — drift that a human reading either file alone would not catch, because each file is internally consistent. It has only been run against those five. Extending it to all 38 pairs is the single highest-value unclaimed job in this repo and is the leading candidate for W36's themed slot.
- Staleness: 27 of 38 builders yellow at 30+ days, 0 red. After this week that drops to roughly 25. At 3 pairs/week the backlog clears around W43.
- Implementation reminder: `.skill` files in `skills/` are **zip archives**, not plain text — unzip to reach `SKILL.md` and `scripts/` before editing, then re-zip in place. (Confirmed again this run; a plain `sed` on a `.skill` file reads binary.)

## Follow-ups

- **Human, blocking rule (a): close #37–#50.** All fourteen are complete. Landing commits: #37/#42 -> a84987a · #43 -> 8c19840 · #44 -> aadcd83 · #45 -> d909fc2 · #46 -> 9d0c927 · #47 -> 6244fd2 · #48 -> 8ba1789 · #49 -> df811d4 · #50 -> 662de6b. #38–#41 landed during W30/W31.
- **Human: decide the staleness metric.** The 30-day stale rule keys on `updated_at`, which this task's own comments reset, so it can never fire. Four issues are stale measured from `created_at`. Options: measure from the newest non-bot event, or fall back to `created_at`.
- **`scripts/audit_descriptions.py` still needs committing.** It has now been written from a heredoc twice (Sunday, and again this run to verify before filing #51). This is precisely the failure mode that cost three repeat fixes before `gen_status.py` was committed. It belongs beside it, checking: generator-framing on reviewers, builder/reviewer **edition agreement**, and the 1024-char limit. Friday DOCS or W36 PLAN.
- ros2 after #52: `nav2-config` is the last ros2 builder at import baseline and follows tf directly.
- Remaining import-baseline builders after this week (6): `nav2-config` (ros2) · `operator-training-matrix` (operational) · `perception-test-catalog` (ai-ml) · `robot-field-acceptance` (v&v) · `wireless-coexistence-plan` (amr) · `zone-conduit-plan` (cybersecurity). W36 should take the amr or cybersecurity one — neither domain has been touched since W30/W31.
- Placeholder-generator defect (`robot-sop`, `machinery-safety-lifecycle-plan`, `fleet-manager-architecture`, `behavior-tree-spec`) remains untouched and still needs a human-scheduled implementation week. It is beyond a single POLISH slot and this task should stop re-listing it as if it were plannable.
- **Human: publish `v2026.08.W4`** after reviewing `RELEASES.md`.
