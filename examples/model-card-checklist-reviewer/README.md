# Example — `model-card-checklist-reviewer.skill`

**What this skill produces:** A confirmation-measures checklist over any Model Card xlsx and produce an audit-ready checklist with auto-filled findings, evidence references, and recommended actions for trustworthiness. Probes the source model card workbook, runs 27 standard checks split across Document Completeness (metadata and provenance), Performance Verification (metrics and slicing), and Fairness Assessment (bias and ethical clarity). Auto-fills FC, LC, PC, NO, or NA and drafts findings for subjective items. Does not modify the source model card. Emitted as a 7-tab workbook with a visual dashboard. Does not modify the source workbook.

**Typical input shape:** A path to a Model Card xlsx (optimal on `model-card-builder` output, best-effort on others), plus optional reviewer metadata (reviewer name, review date, project ID).

**Expected output:** `<source>_checklist.xlsx` — Title and Summary roll-up tabs, a Findings list, and one tab per check category.

**Sample input:** _TODO — add a representative sample input for this skill._

**Sample output:** _TODO — add the generated workbook._

_Stub generated by the autonomous DOCS run on 2026-08-21 as part of the batch pass closing the reviewer-side examples gap (see `docs/weekly/WEEK-2026-W34.md` follow-ups). Content is derived from the skill's own frontmatter and body — it has not been verified against a real run._
