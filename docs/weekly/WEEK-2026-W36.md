# Week 2026-W36 — Plan (autonomous PLAN run)

Generated: 2026-08-31 (Monday)

## Recap (past 7 days)

```
c006688 auto(polish): fix tf-tree-spec world frame, add REP-105 invariants and tf_static trap
2752a53 auto(polish): anchor eoat-spec pair on ten standards, add moment budget and grip retention
097d8b4 auto(polish): fix five reviewer descriptions, correct superseded ANSI edition and IEC 62061 scope
```

**W35 cleared all three targets — third consecutive clean week.** #51 (five reviewer descriptions) landed Tuesday, #53 (eoat-spec) Wednesday, #52 (tf-tree-spec) Thursday. The plan sequenced #51 first because it carried the only wrong-edition safety claim; that ordering held. Capacity 3 has now produced zero carryover three weeks running. **W36 holds at 3.**

## The W35 headline job is done, and it came back nearly clean

W35 named one job the "single highest-value unclaimed job in this repo": extend the builder<->reviewer edition-agreement check from the five compliance pairs it had been run against to all 38. This run did that, and committed the check as `scripts/audit_pair_editions.py` so it stops being re-derived — the same reasoning that put `gen_status.py` in the repo at W34.

**Result across all 38 pairs: 1 mismatch, and it is benign.**

| Class | Count | Reading |
| --- | --- | --- |
| MISMATCH — both halves pin an edition, years differ | **1** | `robot-cell-scope`: builder carries `2011` *and* `2025` because it correctly says "ISO 10218-2:2025 ... Supersedes ISO 10218-2:2011". Not a defect. Verified by reading context, not by the count. |
| ASYMMETRY — one half pins, the other names the standard with no year | **10** | Not a wrong claim. A missing one. |

Two near-misses were checked by hand and cleared:

- `declaration-of-conformity` reviewer cites **ANSI/RIA R15.06-2012** — but inside a *negative* check ("still cites ... where the current editions apply"). Citing a superseded edition in order to flag it is correct behaviour.
- `iso3691-4-risk-assessment` builder cites **ISO 3691-4:2023**, which this task's own standing reference table calls out as `2020`. Checked against ISO: **3691-4:2023 is a real published edition** (ISO cat. 83545) superseding 2020. The builder is right and the task file's table is stale. **See Notes — this needs a human edit to the task file.** A revision (`ISO/CD 3691-4`, cat. 88615) is also in development; worth watching, not worth acting on.

So the drift thesis that has driven three weeks of planning is now **measured rather than assumed, and it is smaller than assumed**. The 10 asymmetries are the residue, and they are a W37 candidate at bounded scope — not this week's work, because none of them is a false claim.

## Target selection — what the priority rules actually returned

- **Rule (a), skills referenced by open issues:** selects nothing, for the **fifth** consecutive week. All 17 open issues (#37-#53) describe work that has already landed.
- **Rule (b), orphan builders:** selects nothing. 38/38 paired since W29.
- **Rule (c), least-recently-touched:** still returns the 2026-05-03 import baseline cohort (120 days). All three targets come from here, and this week they are chosen by a sharper measure than the date.

### The measure this week is body thinness, not staleness

Ranking all 38 builders by non-blank body lines (frontmatter excluded) surfaces a defect class the date sort hides:

| Builder body | Reviewer body | Inverted? | Skill |
| --- | --- | --- | --- |
| **6** | 13 | yes | `iso9283-performance-test` |
| **7** | 10 | yes | `operator-training-matrix` |
| **8** | 11 | yes | `nav2-config` |
| 9 | 11 | yes | `ros2-system-architecture` |
| 9 | 14 | yes | `robot-field-acceptance` |

Every builder under 10 body lines is **inverted** — its reviewer is more substantive than the builder it reviews. A reviewer that knows more than its builder will pass workbooks the builder had no vocabulary to ask for. `eoat-spec`, last week's #53, was the previous holder of this shape at 9 lines; the three thinnest survivors are this week's targets.

(`iso10218-compliance-matrix` at 23/36 lines is the worst *ratio* in the repo and its body is pure boilerplate — "Use this skill when the user mentions ISO 10218, or related requirements" — while its reviewer runs to a real 2025-edition argument. It is the leading W37 target and is deliberately not taken this week; see Notes.)

## Targets (3)

### 1. `nav2-config-builder.skill` — _ros2_ — [#54](https://github.com/jherrodthomas/robotics-skills-suite/issues/54)

At import baseline (2026-05-03, 120 days). 8-line body against an 11-line reviewer.

- **Definition of done:** costmap layers stated as a worked configuration (static / obstacle / inflation / voxel) with the parameter names a user actually sets, not named in the abstract; inflation radius and cost scaling tied to the robot footprint rather than left at defaults; global vs local planner selection stated with the criteria for choosing between them; **frame IDs consistent with the `tf-tree-spec` pair polished 2026-08-27** and link names consistent with `urdf-model-spec` (#49); recovery behaviours and their failure modes enumerated; lifecycle/bringup ordering explicit. Reviewer in lockstep.
- **Take this Tuesday.** This is the second half of the `urdf -> tf -> nav2` sequence W34 opened and W35 continued. Nav2 config references the frames `tf-tree-spec` defines; that pair was polished four days ago and its frame vocabulary is still fresh. Doing nav2 against a stale memory of those decisions means fixing vocabulary twice — the identical argument that justified taking tf before nav2, now cashed in.
- **Not a safety-critical domain** — the mandatory edition-verification step does not fire. Verify the ROS 2 distro baseline against what #49 set on `urdf-model-spec` instead.

### 2. `iso9283-performance-test-builder.skill` — _v&v_ — [#55](https://github.com/jherrodthomas/robotics-skills-suite/issues/55)

**The thinnest builder in the repo: a 6-line body**, against a 13-line reviewer. Last touched 2026-06-18 (74 days).

- **Definition of done:** the ISO 9283 performance characteristics named individually and distinguished — pose accuracy vs pose repeatability is the distinction users get wrong, and unidirectional vs multidirectional repeatability the one they don't know exists; the test cube, the five measurement poses and the ISO test velocities/payloads stated rather than referred to; cycle counts and the warm-up requirement given as numbers; measurement-equipment uncertainty required to be stated relative to the tolerance being claimed; **the boundary against `robot-acceptance-protocol` (FAT/SAT) drawn explicitly** — ISO 9283 measures manipulator performance, not application capability, and a 6-line body is exactly where that conflation gets in. Reviewer in lockstep.
- **Take this Wednesday.** Thinnest file in the suite and the last v&v builder that has never had a real pass.

### 3. `operator-training-matrix-builder.skill` — _operational_ — [#56](https://github.com/jherrodthomas/robotics-skills-suite/issues/56)

At import baseline (2026-05-03, 120 days). 7-line body against a 10-line reviewer. The last operational builder at baseline.

- **Definition of done:** roles enumerated and kept distinct (operator / programmer / maintenance / integrator — ISO 10218-2 assigns different duties to each and a matrix that merges them is unusable); competency rows separated into training delivered vs competency *demonstrated*, with the evidence type per row; **authorized-employee vs affected-employee distinction carried through per OSHA 1910.147(c)(7)**, since this matrix is what `loto-procedure` hands its training obligation to; retraining triggers stated as conditions (role change, procedure change, observed deviation, interval) rather than as a bare interval; record retention and the re-qualification path named. Reviewer in lockstep.
- **Take this Thursday.** Operational is a safety-critical domain: **the mandatory edition-verification step fires.**

## Domain-spread check

Three picks across **ros2, v&v, operational**. No domain carries two targets.

W35 spent compliance/foundation, ros2, cell-design. The repeat is ros2, and — as in W35, which flagged its own ros2 repeat the same way — it is deliberate: it is the closing leg of a three-week sequence whose whole point was to do the frame-defining skills before the frame-consuming one. This is the last ros2 target justified by that sequence; `ros2-system-architecture` (9 lines) is not, and waits its turn on merit.

## Standards / version refresh notes for Tue-Thu POLISH runs

Target 3 is in the safety-critical set, so the **mandatory edition-verification step fires for it**. Targets 1 and 2 are not, but both carry version claims that need checking anyway.

| Target | Editions / baselines to verify and date |
| --- | --- |
| #54 nav2-config | ROS 2 distro baseline **must match what #49 set on `urdf-model-spec`** · Nav2 parameter names against that distro (they have moved between distros) · REP-**105** frame IDs as fixed by #52 |
| #55 iso9283 | ISO 9283:**1998** — confirm still current and confirm it is cited as the performance standard, not as a safety standard · boundary against ISO 10218-2:**2025** integrator acceptance duties |
| #56 operator-training-matrix | OSHA **1910.147(c)(7)** authorized/affected training language · ISO 10218-2:**2025** integrator training duties · ANSI/A3 R15.06-**2025** (2012 (R2017) superseded) |

**Do not assume the builder is right.** Restated for the fourth week, and still the rule that catches things: this week's own audit cleared `iso3691-4` only because the *task file*, not the builder, turned out to hold the stale year.

## Notes

- **Capacity stays at 3.** W32: 3 · W33: 3/3 · W34: 3/3 · W35: 3/3. Three consecutive zero-carryover weeks at the same number is a measured rate, not a guess. Planning above it manufactures noise.
- **`scripts/audit_pair_editions.py` is committed this run.** An earlier ad-hoc version of this check read `.skill` files as plain text — they are zip archives — matched nothing, and reported a perfectly clean repo. That false negative is the reason the check is now a file with the trap documented at the top rather than something retyped from memory each week. Same lesson as `gen_status.py`, one layer deeper: committing the script prevents re-deriving the *bug* as much as the logic.
- **Rule (a) is now five-for-five dead and still needs one human action.** Three new issues (#54-#56) push the open count to 20 while real outstanding work is 3. Until #37-#53 are closed by hand, the highest-priority selection rule in this task contributes nothing to selection. **Eighth consecutive request.**
- **One human edit requested in the task file itself:** the standing reference table lists `ISO 3691-4:2020`. The current edition is **2023**. The builder already tracks 2023 correctly; it is the task file that would cause a future run to "correct" a right answer into a wrong one. This is the first time the task's own reference data, rather than a skill, has been the stale artifact.
- **W37 candidates, in order:** (1) `iso10218-compliance-matrix` — worst builder/reviewer substance ratio in the repo, pure-boilerplate body on the compliance keystone skill, and its own body pins no edition while its reviewer argues the 2025 edition properly; (2) the 10 edition asymmetries, taken as one themed slot at bounded scope — pin the unpinned half, no body rewrites; (3) `ros2-system-architecture` and `robot-field-acceptance`, the two remaining inverted pairs.
- Staleness: 27 of 38 builders yellow at 30+ days, 0 red. After this week, roughly 25. At 3 pairs/week the import-baseline cohort clears around W40 and the full backlog around W44.
