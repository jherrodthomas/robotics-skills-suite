# Example — `ros2-system-architecture-builder.skill`

**What this skill produces:** An audit-ready ROS 2 System Architecture workbook capturing node inventory, topic/service/action catalog, lifecycle node management, executor model, DDS QoS profiles, namespace strategy, and component composition for production ROS 2 systems on a currently supported LTS release (Humble, Jazzy, or Lyrical).

**Typical input shape:** JSON object naming the nodes (package, executable, lifecycle or plain), their interfaces with QoS requirements, executor and composition choices, namespace plan, and the target LTS distro.

**Expected output:** Multi-tab xlsx: title page, node inventory, interface catalog, QoS profile matrix, lifecycle/executor design, namespace and composition plan.

**Sample I/O:** See `sample_input.json` and `sample_output.xlsx` (to be added).

_Stub generated by the autonomous DOCS run on 2026-07-17. Replace with a real worked example when one is available._
