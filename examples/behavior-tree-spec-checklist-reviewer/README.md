# Example — `behavior-tree-spec-checklist-reviewer.skill`

**What this skill produces:** A findings checklist auditing a Behavior Tree specification workbook against BehaviorTree.CPP v4.x and Nav2 practice — control-node choice and tick semantics, reactivity boundaries, action/condition node feasibility, decorator bounds, blackboard scope and subtree port remapping, port type and direction, recovery coverage, and BT XML v4 schema conformance.

**Typical input shape:** The path to a behavior tree specification xlsx (typically produced by `behavior-tree-spec-builder`), plus reviewer name and review date.

**Expected output:** Checklist xlsx with Title, Summary (FC/LC/PC/NO/NA/PENDING roll-up and findings list), and per-area detail tabs. Trees written against BT.CPP v3.x or Groot-1-only palettes are flagged as findings rather than accepted.

**Sample I/O:** See `sample_input.xlsx` and `sample_checklist.xlsx` (to be added).

> **Known gap:** the checklist generator scripts are currently one-line placeholders — see `docs/skill-polish-log/behavior-tree-spec-builder.md`. This stub documents intended behavior.

_Stub generated by the autonomous DOCS run on 2026-07-31. Replace with a real worked example when one is available._
