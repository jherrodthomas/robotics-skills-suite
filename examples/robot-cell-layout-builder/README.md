# Example — `robot-cell-layout-builder.skill`

**What this skill produces:** An audit-ready Robot Cell Layout workbook for an industrial robot cell per `ISO 10218-2:2025` integrator requirements — physical footprint, robot reach envelope, fence and gate placement, perimeter sensors, safety light-curtain locations and response times, conveyor interfaces, work zones, fire egress, and floor markings, with safeguard positioning per `ISO 13855:2024` and minimum safety distances per `ISO 13857:2019`.

**Typical input shape:** JSON object naming the robot model and reach, the cell footprint and dimensions, fence/gate positions, perimeter and light-curtain sensor placements, conveyor/material-handling interfaces, operator work zones, and egress routes.

**Expected output:** Multi-tab xlsx: title page, cell footprint, reach envelope, fence/gate design, perimeter sensor coverage, light-curtain zones (with ISO 13855 approach-speed distances), work zones, egress and floor markings, signoff block.

**Sample I/O:** See `sample_input.json` and `sample_output.xlsx` (to be added).

_Stub generated by the autonomous DOCS run on 2026-07-03. Replace with a real worked example when one is available._
