# Week 2026-W34 — Plan (autonomous PLAN run)

Generated: 2026-08-17 (Monday)

## Recap (past 7 days)

```
c5250c0 auto(triage): verify label coverage, comment five completed issues, log staleness-rule defect
7276a29 auto(release): tag v2026.08.W3 snapshot, write release notes, refresh STATUS
a8ca532 auto(docs): W33 changelog, two v&v example stubs, STATUS refresh
d909fc2 auto(polish): pin safety-io-matrix pair to ISO 13849-1:2023, IEC 62061:2021, add SIL CL cross-reference
6244fd2 auto(polish): anchor robot-acceptance-protocol pair on ISO 10218-2:2025, add reviewer standards baseline
aadcd83 auto(polish): rewrite declaration-of-conformity pair, fork on Directive vs Regulation 2023/1230
e246935 auto(plan): seed W33 with three targets — two carryovers plus v&v beachhead
```

**W33 cleared all three targets — the first fully clean week.** `declaration-of-conformity` (#44) landed Tuesday exactly as the plan instructed, ending a three-week carryover; `robot-acceptance-protocol` (#47) and `safety-io-matrix` (#45) followed Wednesday and Thursday. Setting capacity to 3 instead of 5 produced zero carryover for the first time in the repo's history. **W34 holds capacity at 3.**

**Consequence for target selection: the open-issue queue is now empty of real work.** All 11 open issues (#37–#47) correspond to POLISH passes that have already landed. Priority rule (a) — "skills referenced by open issues" — therefore selects nothing this week, and rule (b) — orphan builders — selects nothing either (38/38 paired since W29). All three targets below come from rule (c), least-recently-touched.

## Targets (3)

All three sit at the **2026-05-03 import baseline** and have never had a POLISH pass.

1. **`robot-hil-test-catalog-builder.skill`** — _v&v_ — [#48](https://github.com/jherrodthomas/robotics-skills-suite/issues/48)
   - **Definition of done:** HIL catalog reconciled with the now-polished `robot-acceptance-protocol` pair — shared test-case ID convention, explicit split between simulated-plant HIL and real-controller FAT, fault-injection cases traceable to `iso13849-plr` PL claims and `safety-io-matrix` dual-channel/OSSD rows. Reviewer in lockstep. `examples/` stub added.
   - **Take this Tuesday.** It continues the v&v beachhead opened last week and sits immediately upstream of the protocol just polished, so the two are cheapest to reconcile while #47 is still fresh.

2. **`urdf-model-spec-builder.skill`** — _ros2_ — [#49](https://github.com/jherrodthomas/robotics-skills-suite/issues/49)
   - **Definition of done:** ROS 2 distro baseline stated explicitly rather than "ROS 2" generically; URDF/xacro vs SDF boundary unambiguous; link/joint/inertial/collision-vs-visual conventions spelled out; `ros2_control` transmission blocks covered; frame-naming pinned to REP-103 and REP-105. Reviewer in lockstep. `examples/` stub added.
   - **Why URDF first:** ros2 is the largest untouched cluster (3 of 5 builders at import baseline). `tf-tree-spec` and `nav2-config` both consume the model URDF defines, so this pick sets vocabulary the other two inherit — doing it in the reverse order would mean polishing twice.

3. **`dataset-documentation-builder.skill`** — _ai-ml_ — [#50](https://github.com/jherrodthomas/robotics-skills-suite/issues/50)
   - **Definition of done:** Datasheet sections reconciled with the polished `model-card` pair so a dataset ID cited from a model card resolves to a real field here. Governance references named and dated (ISO/IEC 42001:2023, ISO/IEC 23894:2023, EU AI Act Regulation 2024/1689 Art. 10). Robotics provenance fields covered: sensor modality, capture rig, calibration state, operating-envelope coverage. Reviewer in lockstep. `examples/` stub added.
   - **Why:** `model-card` was polished 2026-07-02 but its upstream datasheet was not — a known chain-consistency gap inside ai-ml.

## Domain-spread check

3 picks across 3 distinct domains: **v&v, ros2, ai-ml**. No domain repeated within the week, and none carries 2+ targets.

**Judgement call on the v&v repeat.** W33's follow-ups floated a *themed v&v week* (all remaining v&v builders in one pass). That would have put 2–3 targets in one domain, against the spread rule in the task definition. The compromise taken here: keep one v&v pick — the one with the strongest adjacency to last week's work — and spend the other two slots opening the two clusters that have never been touched at all. Spread rule wins; the v&v theme gets one slot, not the week.

## Standards / version refresh notes for Tue–Thu POLISH runs

None of the three targets is in the safety-critical set (foundation / compliance / cobot / amr / cell-design / cybersecurity), so the mandatory edition-verification step does not fire. **All three still carry version-dated references that should be checked anyway** — the failure mode is the same, just with a different standards body:

| Skill | References to verify and date |
| --- | --- |
| robot-hil-test-catalog | ISO 10218-2:**2025** (integrator verification) · ISO 9283:**1998** · IEC 61508-3 for HIL model validity, if cited |
| urdf-model-spec | ROS 2 distro baseline (state which) · REP-**103** units/axis · REP-**105** frame IDs · SDF version if the URDF/SDF boundary is discussed |
| dataset-documentation | ISO/IEC 42001:**2023** · ISO/IEC 23894:**2023** · EU AI Act Regulation **2024/1689** Art. 10 |

## Notes

- **Capacity held at 3.** W30: 2 cleared. W31: 2. W32: 3. W33: 3 of 3, zero carryover. Three is the observed rate; planning above it only manufactures carryover noise.
- **The open-issue count is now a broken signal and needs one human action.** Eleven issues open, eleven of them done. Three new issues (#48–#50) push the count to 14 while real outstanding work is 3. This task is forbidden from closing issues, and Sunday TRIAGE has now flagged the same thing multiple weeks running (a5356c7, c5250c0). Until #37–#47 are closed by hand, priority rule (a) is dead weight in this plan — it selected nothing this week and will keep selecting nothing.
- Reviewer staleness remains the structural debt: 28 of 38 builders 🟡 at 30+ days, most of their reviewers still at the 2026-05-03 import baseline. Lockstep commits are the only lever moving it; at 3 pairs/week the 28 clears around W43.
- Examples coverage is stuck at 38/76 — every reviewer half lacks a README. All three DoDs above include the builder-side stub; the reviewer-side gap is a separate DOCS-run job, captured below.
- Implementation reminder for POLISH runs: `.skill` files in `skills/` are **zip archives**, not plain text — unzip to reach `SKILL.md` and `scripts/` before editing, then re-zip in place.

## Follow-ups

- **Human, blocking the value of rule (a): close #37–#47.** All eleven are complete. Known landing commits: #37/#42 → a84987a, #43 → 8c19840, #44 → aadcd83, #45 → d909fc2, #46 → 9d0c927, #47 → 6244fd2; #38–#41 landed during W30/W31 polish runs.
- ros2 after #49: `tf-tree-spec` and `nav2-config` remain at import baseline and should follow in W35 in that order (tf before nav2 — Nav2 config references frames).
- v&v after #48: `robot-field-acceptance` is the last v&v builder at import baseline; `iso9283-performance-test` was touched 2026-06-18.
- ai-ml after #50: `perception-test-catalog` is the last ai-ml builder at import baseline. Consider pairing it with #50's outcome since both describe sensor-data coverage.
- DOCS run (Friday) should consider a batch pass creating `examples/<reviewer>/README.md` stubs, rather than the 2–3 per week the current rule produces. At 2/week the 38 missing reviewer stubs take until 2027.
