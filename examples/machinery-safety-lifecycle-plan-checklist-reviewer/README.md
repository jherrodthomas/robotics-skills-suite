# Example — `machinery-safety-lifecycle-plan-checklist-reviewer.skill`

**What this skill produces:** A completed review checklist auditing any Machinery Safety Lifecycle Plan xlsx — lifecycle-phase coverage from concept through validation and modification against `ISO 12100:2010` risk-assessment iteration, `ISO 13849-1:2023` and `IEC 62061:2021` functional-safety lifecycle requirements, and `EU Machinery Regulation 2023/1230` transition framing, including roles, safety integrity targets (PLr/SIL), work products, gate reviews, and verification methods. Does not modify the source plan.

**Typical input shape:** Path to a lifecycle plan xlsx (optimal on `machinery-safety-lifecycle-plan-builder` outputs, best-effort on others).

**Expected output:** Checklist xlsx with per-requirement ratings, evidence references, findings, recommended actions, and a visual dashboard.

**Sample I/O:** See `sample_input.json` and `sample_output.xlsx` (to be added).

_Stub generated by the autonomous DOCS run on 2026-07-24. Note: the packaged probe scripts are currently placeholders (see docs/skill-polish-log/machinery-safety-lifecycle-plan-builder.md); replace this stub with a real worked example once implemented._
