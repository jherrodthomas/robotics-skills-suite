# Triage — 2026-09-20 (Sunday)

Autonomous TRIAGE run. **The issue board is empty**, so the usual work of this
mode (infer labels, apply them, fire the 30-day quiet rule) had nothing to act
on. This report substitutes repository-level triage: what the audit scripts say
is actually wrong, and what tomorrow's PLAN run should pick up.

## 1. Board state

| Metric | Value |
| --- | --- |
| Open issues | **0** |
| Last issue activity | 2026-09-11 — #45–#59 all closed in one sweep |
| Labels applied this run | 0 (nothing open) |
| Issues needing human triage | 0 |

All fifteen `weekly-target` issues from W32–W37 were closed by hand at
2026-09-11T20:22Z. No issues have been opened since. Nothing was labelled,
commented on, or left ambiguous this run.

**Label taxonomy note:** the repo carries both the GitHub defaults (`bug`,
`documentation`, `enhancement`, `question`, `duplicate`, `invalid`, `wontfix`,
`good first issue`, `help wanted`) and the standup taxonomy (`skill-bug`,
`docs`, `description-quality`, `new-skill`, `ci`, `chain-break`,
`reviewer-finding`) plus all ten domain labels. `bug`/`skill-bug` and
`documentation`/`docs` are near-duplicates. Not deleted — label deletion is a
human call.

## 2. Missed run — W38 has no plan

There is no `docs/weekly/WEEK-2026-W38.md` and no journal entry for
**2026-09-14 (Monday)**. The PLAN run for W38 did not fire. Consequences:

- W38 had no stated targets and opened no `weekly-target` issues.
- Tue–Thu POLISH runs (09-15, 09-16, 09-17) fell through to their fallback
  priority — least-recently-touched builder — rather than working a planned
  target. They still produced useful passes, but unplanned ones.
- The Sunday 2026-09-13 TRIAGE run is also absent from the journal.

Tomorrow (2026-09-21) is a PLAN day and will open W39. W38 is left unplanned on
purpose — backfilling a plan for a week already worked would be fiction.

## 3. Repository triage — what the audits actually found

### 3.1 Reviewer implementation depth (`scripts/audit_reviewer_impl.py`)

This is the largest structural gap in the repo and it is **not represented on
the board at all**.

| Tier | Meaning | Count |
| --- | --- | --- |
| A | Real generator, real checks | **6** / 38 |
| B | Stub generator, 0 checks | **16** / 38 |
| C | No generator at all | **16** / 38 |

Tier A (the only fully implemented reviewers): `dataset-documentation`,
`iec62443-risk-assessment`, `model-card`, `ot-asset-inventory`,
`perception-test-catalog`, `zone-conduit-plan`.

Also: **15 SKILL.md files cite a `references/` path that is not in the archive.**
Every one of those is a broken promise to whoever runs the skill.

STATUS.md reports 38/38 builders paired at 100% — true by filename, but the
pairing is nominal for 32 of 38. Worth saying plainly: *paired* and
*implemented* are different metrics, and only the first is currently tracked.

### 3.2 Standard-edition drift (`scripts/audit_pair_editions.py`)

38 pairs scanned · **2 MISMATCH** · **9 ASYMMETRY** · 18 individual findings
across 11 pairs.

MISMATCH (both halves pin, years disagree — the more serious class):

- `robot-cell-scope` — ISO 10218-2: builder `['2011','2025']` vs reviewer `['2025']`
- `zone-conduit-plan` — IEC 62443: builder `['2013','2019','2020']` vs reviewer `['2013','2020']`

ASYMMETRY (one half pins, the other is silent) affects: `cobot-hand-guiding` (3),
`interlock-estop-architecture` (4), `robot-cell-layout` (3), `ssm-plan` (3),
`declaration-of-conformity`, `iec62443-risk-assessment`,
`iso3691-4-risk-assessment`, `robot-cell-scope`, `robot-sop`.

Note that W37 issue #59 targeted "16 edition asymmetries across 8 pairs" and was
closed on 2026-09-11. The count today is 18 findings across 11 pairs — so either
the sweep did not close them, or the audit widened. **Flagging rather than
assuming**: this warrants a human look before another sweep is scheduled.

### 3.3 Staleness

29 of 38 builders are 🟡 stale (30+ days). Oldest:

| Builder | Days stale |
| --- | --- |
| `wireless-coexistence-plan-builder` | 140 |
| `perception-test-catalog-builder` | 140 |
| `operating-envelope-builder` | 109 |
| `iso15066-biomechanical-limits-builder` | 108 |
| `iso12100-risk-assessment-builder` | 103 |
| `interlock-estop-architecture-builder` | 95 |
| `iso13849-plr-builder` | 89 |
| `iec62443-risk-assessment-builder` | 88 |

## 4. Shortlist for tomorrow's PLAN run (W39)

Not opened as issues — PLAN mode owns issue creation. Offered as input.

1. **`interlock-estop-architecture` pair** (cell-design) — 95d stale *and* carries
   4 of the 18 edition asymmetries. Highest density of known defect.
2. **`robot-cell-scope` ISO 10218-2 mismatch** (foundation) — a true MISMATCH,
   builder still carrying the superseded 2011 edition alongside 2025.
3. **`iso15066-biomechanical-limits` pair** (cobot) — 108d stale, safety-critical
   domain, ISO/TS 15066:2016 edition unverified this quarter.
4. **The 15 missing `references/` paths** — one sweep, mechanical, closes a
   category of broken skill rather than one instance.
5. **Domain spread check:** the five above hit cell-design, foundation, cobot and
   cross-cutting. No ros2 target — deliberate, ros2 took three passes in W38.

## 5. Follow-ups for the human

- **The board is empty.** Autonomous runs cannot prioritise against open issues
  when there are none; POLISH has been running on the least-recently-touched
  fallback for a week. Tomorrow's PLAN will refill it.
- **Why did 2026-09-14 not run?** Two consecutive scheduled runs (09-13 TRIAGE,
  09-14 PLAN) are missing from the journal. Worth checking the schedule.
- **Tier B/C reviewers (32 of 38) are the real debt**, and it is invisible in
  STATUS.md. Consider whether STATUS should carry an implementation column.
- **Duplicate labels** (`bug`/`skill-bug`, `documentation`/`docs`) — delete or keep?
