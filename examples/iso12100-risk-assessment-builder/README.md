# Example — `iso12100-risk-assessment-builder.skill`

**What this skill produces:** An `ISO 12100:2010` machinery risk-assessment workbook — hazard identification across the machine lifecycle, risk estimation (severity / frequency / probability / avoidance), and the three-step risk-reduction record (inherently safe design, safeguarding, information for use) with residual-risk tracking.

**Typical input shape:** JSON object naming the machine, its limits (use, space, time), task/hazard pairs per lifecycle phase, the risk-estimation parameters per hazard, and the planned protective measures.

**Expected output:** Multi-tab xlsx: title page, document control, machine limits, hazard catalog, risk-estimation worksheet, risk-reduction / residual-risk log, signoff block.

**Sample I/O:** See `sample_input.json` and `sample_output.xlsx` (to be added).

_Stub generated by the autonomous DOCS run on 2026-06-19. Replace with a real worked example when one is available._
