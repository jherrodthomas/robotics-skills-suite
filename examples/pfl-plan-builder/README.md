# Example — `pfl-plan-builder.skill`

**What this skill produces:** An audit-ready Power and Force Limiting plan workbook per `ISO 10218-1:2025/-2:2025` and `ISO/TS 15066:2016` — PFL safety function design, force and pressure measurement results against per-body-region biomechanical limits, contact scenarios with risk reduction strategy, and verification protocol.

**Typical input shape:** JSON describing the cobot application (robot model, payload, contact scenarios per body region, measured force/pressure values, safety function configuration).

**Expected output:** Multi-tab xlsx PFL plan with per-body-region limit comparison and pass/fail verification.

**Sample I/O:** See `sample_input.json` and `sample_output.xlsx` (to be added).

_Stub generated by the autonomous DOCS run on 2026-07-24. Note: the packaged generator is currently a placeholder (see docs/skill-polish-log/pfl-plan-builder.md); replace this stub with a real worked example once the generator is implemented._
