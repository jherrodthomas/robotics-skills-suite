# Example — `loto-procedure-builder.skill`

**What this skill produces:** An audit-ready Lockout/Tagout procedure workbook per `OSHA 29 CFR 1910.147` and `ISO 14118` covering energy source inventory, energy-isolating devices and isolation points, lock and tag application steps, verification of zero-energy state, group lockout coordination, and the re-energization sequence for industrial robot maintenance and service.

**Typical input shape:** JSON object naming the machine/cell, its energy sources (type, magnitude, isolation point), energy-isolating devices, lock/tag hardware, authorized and affected employee roles, and the group-lockout coordinator.

**Expected output:** Multi-tab xlsx: title page, energy source inventory, isolation point map, step-by-step lock/tag application, zero-energy verification protocol, group lockout coordination, re-energization sequence.

**Sample I/O:** See `sample_input.json` and `sample_output.xlsx` (to be added).

_Stub generated by the autonomous DOCS run on 2026-07-17. Replace with a real worked example when one is available._
